import type { CandidateFilters } from "../types/candidates";

export const DEFAULT_FILTERS: CandidateFilters = {
  split: "test",
  reviewStatus: "all",
  reviewed: "all",
  acquisitionDate: "all",
  scene: "all",
  search: "",
  sortBy: "fusion",
};
